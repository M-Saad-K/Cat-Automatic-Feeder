import camera_op
from camera_op import animal_detected

"""
# Implement a timer
                    length = 0
                    start = time.time()
                   
                    image_filename = capture_photo(img, save_directory)
                    send_ntfy_notification(className, image_filename)

                    # Notification is sent, told that will wiat for period for another notification
                    while length != 60:
                        end = time.time()   
                        length = end - start
"""

# Run Live Feed
# Detect Cat or Bird
# If Cat -> user command (own sep file)
# If Bird -> user command (own sep file)
# Dispense auotmatically

